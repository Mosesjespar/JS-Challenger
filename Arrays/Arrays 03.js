// Write a function that takes an array (a) as argument
// Extract the last 3 elements of a
// Return the resulting array
function myFunction(a) {

              return a.slice(-3);


              //the slice() method removes elements of an array
              //the parameter -3 removes the last 3 elements.....if it was 3,it would remove the first 3
}
